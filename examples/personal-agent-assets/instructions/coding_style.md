# Coding Style Guide

Keep changes small and aligned with the existing codebase.

- Prefer existing local patterns over new abstractions.
- Keep parser, validator, registry, graph, and CLI responsibilities separate.
- Make JSON output deterministic for tests and demos.
- Do not add future-phase features while completing Phase 1 work.
